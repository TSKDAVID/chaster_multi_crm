# Chaster Brain Dashboard

TypeScript control UI for Chaster Brain runtime operations.

## Run

1. Copy `.env.example` to `.env`.
2. Install dependencies:
   - `npm install`
3. Start:
   - `npm run dev`
